datasource db {
  provider = "postgresql"
  url      = env("PG_URL_PRISMA")
}

generator kysely {
  provider = "prisma-kysely"

  output   = "../src/generated/prisma/kysely"
  fileName = "types.ts"
}


model Test {
  id           Int            @id @default(autoincrement())
  
}

